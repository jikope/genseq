#include <genseq/NotationReader.hpp>
#include <spdlog/spdlog.h>
#include <vector>

std::vector<std::string> NotationReader::PDFToNotation(const sfs::path& pdf_path) {
    std::vector<std::string> notation;

    return notation;
}
